{
  "name": "mini-rollup-contract",
  "version": "1.0.0",
  "description": "Mini-Rollup smart contract for TON blockchain",
  "scripts": {
    "build": "blueprint build",
    "test": "jest",
    "deploy": "blueprint run deploy",
    "deploy:testnet": "blueprint run deploy --testnet"
  },
  "devDependencies": {
    "@tact-lang/compiler": "^1.6.13",
    "@ton/blueprint": "^0.44.2",
    "@ton/core": "^0.63.1",
    "@ton/crypto": "^3.3.0",
    "@ton/sandbox": "^0.42.0",
    "@ton/test-utils": "^0.12.0",
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testPathPattern": "tests/.*\\.spec\\.ts$"
  },
  "dependencies": {
    "@ton/ton": "^16.2.4"
  }
}
